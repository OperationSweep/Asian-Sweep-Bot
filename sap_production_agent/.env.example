# Copy to .env. Never commit the real file.

# Only needed if the automation must log in to SAP itself. Preferred approach:
# log in to SAP GUI by hand, and the automation attaches to your session
# without ever handling a password.
SAP_USER=
SAP_PASSWORD=

# Only needed when mistral.enabled is true in config.yaml.
MISTRAL_API_KEY=
